import 'package:flutter/material.dart';

class AppColors {
  AppColors._();
  // Primary Colors
  static const white = Colors.white;
  static const black = Colors.black;
  static const grey = Colors.grey;
  static const orange = Color(0Xfff87265);

  // Background color
  static const lightPrimaryBg = Color(0xFFf8f9fa);
  static const darkPrimaryBg = Color(0xFF1a2530);
  static const painter = Color(0xFFb3e0ff);

  // Button colors
  static const primaryButton = Color(0xFF0b6efe);
  static const secondaryButton = Color(0xFF5b9ee1);
  static const disableButton = Color(0xFFb6d3ff);

  // Text colors
  static const primaryText = Color(0xFF1a2530);
  static const secondaryText = Color(0xFF707b81);
  static const tertiaryText = Color(0xFF5b9ee1);
  static const whiteText = Color(0xFFF2FCFF);

  // transparent
  static const transparent = Colors.transparent;
}
